name: infinity_app
description: "App for Infinity Coaching Center."

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: 3.2.0
  cloud_firestore: 5.1.0
  firebase_auth: 5.1.2
  provider: 6.1.2
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true